version: 2.1

orbs:
  node: circleci/node@5.0.3

jobs:
  deploy:
    docker:
      - image: cimg/base:2022.10
    steps:
      - checkout
      - attach_workspace:
          at: dist/dist
      - add_ssh_keys:
          fingerprints:
            - "ec:46:2b:fe:a9:8f:e4:68:26:6a:87:08:e3:87:61:03"
      - run: sh ./scripts/github-pages-deploy.sh dist

workflows:
  ci-cd:
    jobs:
      - node/run:
          filters:
            branches:
              ignore:
                - gh-pages
                - gh-pages-test
          version: '8.17.0'
          pkg-manager: yarn
          yarn-run: build
          post-steps:
            - persist_to_workspace:
                root: .
                paths:
                  - dist/*
      - deploy:
          requires:
            - node/run
          filters:
            branches:
              only: circleci-project-setup
